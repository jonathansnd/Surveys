<li>
	<%= image_tag(user.thumbnail+"?oauth_token="+@serviceauth.token,:size=>"30x30") %>
	<%= link_to user.name, user %>
	<% if current_user.admin? %>
  	| <%= link_to "delete", user, :method => :delete, :confirm => "You sure?", :title => "Delete #{user.name}" %>
	<% end %>
</li>